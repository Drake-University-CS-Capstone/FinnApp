# Copy this file to `.env` in the backend directory and adjust values.
#
# The application expects a single DATABASE_URL compatible with SQLAlchemy.

###############################
# Azure SQL (cloud example)
###############################
#DATABASE_URL="mssql+pyodbc://YOUR_USER:YOUR_PASSWORD@yourserver.database.windows.net:1433/yourdatabase?driver=ODBC+Driver+18+for+SQL+Server&Encrypt=yes&TrustServerCertificate=no&Connection+Timeout=30"

###############################
# Local SQL Server via Docker
###############################
# If you're using the docker-compose.yml in the repo, the SQL Server
# container is reachable as `sqlserver` from within other containers,
# and `localhost` from your host machine.
#
# From host (e.g. running Flask directly on your machine):
DATABASE_URL="mssql+pyodbc://sa:Your_password123@localhost:1433/appdb?driver=ODBC+Driver+18+for+SQL+Server&Encrypt=no&TrustServerCertificate=yes"
#

